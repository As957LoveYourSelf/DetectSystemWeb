package com.postdesign.detectsystem.service.algorithmService;

import java.awt.*;

public interface ImageStyleTranService {
    Image animalStyle(Image input, Integer type);
    Image anyStyle(Image context, Image style);
}
